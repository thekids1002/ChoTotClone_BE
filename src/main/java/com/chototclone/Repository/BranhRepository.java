package com.chototclone.Repository;

import com.chototclone.Entities.Brand;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BranhRepository extends JpaRepository<Brand, Long> {
}
